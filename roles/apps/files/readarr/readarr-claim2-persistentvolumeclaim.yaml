apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: readarr-claim2
  name: readarr-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
