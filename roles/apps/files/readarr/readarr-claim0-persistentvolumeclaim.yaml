apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: readarr-claim0
  name: readarr-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
